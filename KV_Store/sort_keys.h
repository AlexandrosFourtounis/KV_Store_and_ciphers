/*
Sorts the provided key-value pairs in asceding key order.
    @param
        - keys: a pointer to the key-value pairs.
    @return
        - a pointer to the sorted key-value pairs.
*/
int **sort_keys(int **keys);